function openchildfun() {
    // open window : contains 
    // open("url for target window","target","dimension");
    childone=open("childwin.html", "", "width=600,height=600");// return reference to opened window
}

